function Main(props) {
  return (
    <div>
      <h1>
        This is a main element, {props.character}, {props.character}
      </h1>
      <h2>This is an H2 element inside Main Js{props.character}</h2>
    </div>
  );
}

export default Main;
